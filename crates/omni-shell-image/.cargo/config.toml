# =============================================================================
# omni-shell-image — cargo target configuration
# =============================================================================
# Mirrors the `omni-driver-net-virtio-image/.cargo/config.toml` rationale:
# the `x86_64-unknown-none` target spec produces ET_DYN/PIE ELFs by default,
# which is what the kernel's `spawn_from_elf` expects. No relocation-model
# overrides are required; the file is intentionally minimal so the workspace
# `.cargo/config.toml` rustflags (force-soft SIMD cfgs) merge cleanly.
# =============================================================================

# This file is intentionally empty for the `x86_64-unknown-none` target.
